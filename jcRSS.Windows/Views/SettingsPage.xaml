<Page
    x:Class="jcRSS.WindowsUniversal.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:jcRSS.WindowsUniversal.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Pivot>
        <Pivot.Background>
            <ImageBrush ImageSource="/Assets/background.jpg" Opacity="0.5" />
        </Pivot.Background>

        <PivotItem Header="Settings" ScrollViewer.VerticalScrollBarVisibility="Hidden">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                   
                <RelativePanel Grid.Row="0">
             
                </RelativePanel>

                <RelativePanel Grid.Row="1" Margin="0,10,0,0">
                  
                </RelativePanel>

                <RelativePanel Grid.Row="2" Margin="0,10,0,0">
                    
                </RelativePanel>
            </Grid>
        </PivotItem>
    </Pivot>
</Page>